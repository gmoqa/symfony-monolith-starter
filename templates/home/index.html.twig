{% extends 'base.html.twig' %}
{% block body %}
    <div class="starter-template">
        <h1>Symfony4 Monolith Base</h1>
        {% if app.user %}
            <h5>Hi! {{ app.user.firstName }}</h5>
        {% endif %}
        <p  class="lead">
            The monoliths sometimes have things in common.
        </p>
        <p class="lead">
            Your project already got security, fixtures, cruds, pagination, <br>
            chart.js, pdf/xlsx exports, elasticsearch, rabbitMQ <br>
            and latest version of Bootstrap (4.1.3)<br><br>
            Built with latest version of Symfony ({{ symfony_version() }})<br>
            MIT License
        </p>
        <p class="lead">
            So now, destroy this view, and let's rock 'n' roll.
        </p>
    </div>
{% endblock %}
